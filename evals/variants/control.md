# Control Variant

Run the task with the agent's normal behavior.

Do not add Tokenwise, Superpowers, or CodeGraph-specific workflow instructions.
Use the agent's default tools and default coding process.

Record metrics from the run log.

